spring:
  cloud:
    propertiesvolume:
      prefixed: true
      paths: /mnt/secrets/s2s,/mnt/secrets/rd
      aliases:
        s2s.microservicekey-rd-professional-api: S2S_SECRET
        rd.s2s-url: S2S_URL
        rd.s2s-secret: idam.s2s-auth.totp_secret
        rd.professional-api-POSTGRES-PASS: spring.datasource.password
        rd.AppInsightsInstrumentationKey: azure.application-insights.instrumentation-key
        rd.LD-SDK-KEY: LD_SDK_KEY

