spring:
  cloud:
    propertiesvolume:
      prefixed: false
      paths: /mnt/secrets/s2s,/mnt/secrets/rd
      aliases:
        s2s.microservicekey-rd-professional-api: S2S_SECRET
        rd.s2s-url: S2S_URL
        s2s-secret: idam.s2s-auth.totp_secret
        professional-api-POSTGRES-PASS: spring.datasource.password
        AppInsightsInstrumentationKey: azure.application-insights.instrumentation-key
        rd.OAUTH2-CLIENT-SECRET: OAUTH2_CLIENT_SECRET